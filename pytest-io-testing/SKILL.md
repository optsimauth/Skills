---
name: pytest-io-testing
description: 用输入输出对（input/output pairs）为AI生成的Python代码写pytest测试，让不看代码实现的人也能通过跑测试判断代码对错。当用户要求"写代码后帮我测一下"、"给这个函数写测试"、"验证这段代码对不对"、"用测试用例检查"、或者提到"pytest"、"单元测试"时，必须使用本skill，即使用户没有明确说"skill"这个词。每写完一个新的源文件或修改一个已有函数，都应主动使用本skill为其配套测试。
---
# Pytest 输入输出测试

## 核心原则

**每个源文件对应一个同名测试文件，测试只做一件事：给输入、比对输出。**
用户不看实现代码，只看 `pytest -v` 的结果（PASSED/FAILED + 期望值 vs 实际值）来判断代码对错。

## 目录约定

```
project/
├── src/
│   └── xxx.py          ← 业务代码
└── tests/
    └── test_xxx.py      ← 对应的测试，文件名必须是 test_ + 源文件名
```

新建/修改 `src/xxx.py` 时，同步创建或更新 `tests/test_xxx.py`。不要把测试和源码写在同一个文件里。

## 写测试的标准写法

用 `@pytest.mark.parametrize` + 一份 `(输入..., 期望输出)` 列表，不要为每组数据单独写一个函数。

```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from xxx import 被测函数

# 核心：只维护这一份 (输入, 期望输出) 列表，覆盖正常/边界/异常场景
cases = [
    (输入1, 输入2, 期望输出),
    # ... 尽量覆盖：正常值、边界值(0、空、极大极小)、异常值
]

import pytest

@pytest.mark.parametrize("参数名1,参数名2,expected", cases)
def test_被测函数(参数名1, 参数名2, expected):
    实际 = 被测函数(参数名1, 参数名2)
    assert 实际 == expected, f"输入({参数名1},{参数名2})，期望{expected}，实际{实际}"
```

**关键规则**：

- `parametrize` 第一个参数是字符串，逗号分隔的名字必须和下面函数签名的参数名**完全一致**、数量一致，否则 pytest 会在收集阶段直接报错。
- 每个 assert 都要写 `f"输入...期望...实际..."` 这样的失败信息，让用户不看代码就知道错在哪。
- 如果测试涉及状态变化（如账户锁定、计数器），涉及"行为描述"而非纯输入输出映射时，测试函数名用中文描述期望的业务行为（如 `test_连续失败5次后应锁定`），而不是 parametrize 数值型输入输出。两种场景都可能出现在同一个项目里，按情况选择。

## 运行方式

```bash
# 跑全部测试
pytest -v

# 只跑某个文件
pytest tests/test_xxx.py -v

# 只跑名字包含关键词的用例
pytest -k "关键词"

# 只重跑上次失败的
pytest --lf
```

## 完成后的动作

1. 写完/改完测试后，主动执行 `pytest -v` 跑一遍，把结果（尤其是 FAILED 的详情）展示给用户，不要只说"我写好测试了"。
2. 如果测试失败，先看是测试数据写错了还是代码本身有问题，明确告诉用户结论，而不是抛一堆报错让用户自己判断。
3. 不要引入额外的测试管理框架、数据库、CI配置等——除非用户明确要求，保持最简方案。
